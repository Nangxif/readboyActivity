// filter.wxs
var dateFormat = function (date) {
  var dates = date.substring(0,10);
  return dates;
}


var acticityFilter = function(date){
  return getDate() - getDate(date)
}
/*
 * 导出
 */
module.exports = {
  dateFormat: dateFormat,
  acticityFilter: acticityFilter
}
